//just for reference to validate the api's

//const Joi = require('@hapi/joi')

// var schema={};
// schema.addMovieSchema = Joi.object({
//   director: Joi.string().required(),
//   name: Joi.string().required(),
//   hero:Joi.string(),
//   heroine:Joi.string()
// })

// schema.bookMovieSchema = Joi.object({
//   seats: Joi.array().min(1).max(6).required(),
//   hallname: Joi.string().required(),
//   showtime:Joi.string().required()
// })

//module.exports = schema;
